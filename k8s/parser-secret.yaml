apiVersion: v1
kind: Secret
metadata:
  name: parser-secret
  labels:
    app: steam-monitor
    component: parser
    tier: backend
type: Opaque
stringData:
  # Замените на реальные значения перед деплоем (kubectl create secret ... --dry-run=client)
  KAFKA_SASL_PASSWORD: "changeme"
